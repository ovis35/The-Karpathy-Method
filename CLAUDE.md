# Karpathy 全域規則

## 工作區與權限
- `D:\Karpathy`：Andrej Karpathy 知識庫工作區，可依任務讀寫。
- `D:\Truth_IC`：投資公司知識庫工作區，可依任務讀寫。
- `D:\Truth_IC\raw`：原始來源層，唯讀，不可修改、刪除、搬移或覆寫。
- `D:\Truth_IC\raw\assets`：原始來源附件層，唯讀，不可修改。
- `D:\Truth_IC\wiki`：衍生知識層，可依任務讀寫。
- `D:\Brain`：唯讀，不可寫入。
- `D:\Gravity`：唯讀，不可寫入。
- `D:\System`：除非任務明確要求，否則不操作。
- `C:\Users\user\Desktop\00_dev_projects`：除非任務明確要求，否則不操作。
- `C:\Users\user\Desktop\01_prod_projects`：僅在使用者明確要求時操作。

## 宗旨
`Karpathy` 是一套由 Claude Code 維護的研究型 wiki，建立在不可修改的原始來源之上。

- `raw/` 是原始來源層，也是 source of truth。
- `wiki/` 是由 Claude Code 編譯、維護、交叉連結的知識層。

人類閱讀與審閱 wiki；Claude Code 負責整理、編譯、增量維護與結構修補。

## 核心原則
Claude Code 是執行型協作夥伴，不是即興發揮者。  
預設行為：先確認、守範圍、求簡潔、不杜撰、可追溯。

## 誠實
- 不可杜撰無法讀取、驗證或確認的內容。
- 不可虛構來源內容、頁面狀態、完成進度或結論。
- 若無法讀取、驗證或確認，必須立即明說。
- 若某段內容是整理、推論或暫時性判斷，必須明確標示。
- 若不確定，就直接說不確定。

## 範圍控制
- 修改前先確認目前工作目錄、目標 vault、目標資料夾與目標頁面。
- 預設只在 `D:\Karpathy 內操作。
- 預設只修改本次任務明確涉及的頁面、索引與 log。
- 不可因名稱相似、自身記憶或推測，自行跨到其他 vault、repo 或工作區。
- 對 `raw/` 與 `raw/assets/` 只可讀取，不可寫入。
- 若任務跨 vault、跨專案或範圍不明，先檢查再行動。

## 簡潔原則
- 預設採最簡單可行的知識整合方式。
- 優先更新既有頁面，不因一時方便大量新增新頁。
- 除非必要，不引入新的資料夾層級、命名體系、schema 或 workflow。
- 避免為了形式整齊而重寫大量既有內容。

## 變更紀律
- 採最小必要改動原則。
- 先讀相關頁面與來源，再進行修改。
- 建立新頁面前，先確認是否已有：
  - 同名頁面
  - 高度相似頁面
  - 可擴充的上位頁面
- 未被明確要求時，不做大型重構、廣泛改名、整體搬移或批次重寫。
- 短期問答、一次性問題或低價值摘要，除非具有長期價值，否則不要沉澱為獨立頁面。

## 目錄角色
- `raw/`：原始來源文件與筆記。不可修改。
- `raw/assets/`：來源附件與媒體檔案。不可修改。
- `wiki/`：衍生知識頁面，只能寫在這裡。
- `wiki/index.md`：wiki 導覽入口。
- `wiki/log.md`：append-only 操作紀錄，不回頭覆寫。

## 知識分層
- 原始層：`raw/` 與 `raw/assets/`，只能讀取與引用。
- 知識層：`wiki/`，可寫入摘要、主題整理、比較、時間線、綜合頁。
- 推論層：若某段不是來源原文明示，而是 Claude Code 的整理、比較或推論，必須可辨識，不可混寫成來源事實。

## 頁面撰寫慣例
所有正式 wiki 頁面使用 Markdown，並盡量包含：
- YAML frontmatter
- 明確標題
- 頁面摘要
- 相關頁面連結
- 可追溯來源
- 不確定性、衝突點或待確認事項

除非我明確要求，否則請使用繁體中文撰寫 wiki 頁面。  
檔名、標題與摘要優先使用繁體中文；必要時可保留英文原詞於括號中。

### Frontmatter 規範
正式頁面建議使用：

```yaml
---
tags: [主分類, 次分類]
type: property | topic | entity | directory | timeline | index | comparison | synthesis | note
created: YYYY-MM-DD
updated: YYYY-MM-DD
source_type: raw | raw+external
source_files:
  - raw/xxx.md
  - raw/yyy.pdf
confidence: high | medium | low
status: active | draft | needs_review
---
```

## 頁面命名與建立規則

- 優先更新既有頁面，只有在確認沒有合適頁面時才建立新頁。
- 新頁面標題應簡潔、穩定、可重複引用。
- 避免一次性問題句、聊天式命名或僅差些微措辭的重複頁。
- 若某內容可作為既有頁面的一節，優先合併，不急著獨立。
- 適合獨立成頁的內容包括：穩定概念、人物或組織、主題整理、比較分析、時間線、綜合性結論、長期可重複引用的核心問題。

## 來源引用規則

- 每個重要頁面應盡量在文末加入「來源」區塊。
- 來源優先使用 vault 內相對路徑。
- 若同時使用 `raw` 與外部資料，必須分開列示。
- 若某段內容主要是 Claude Code 的綜合整理或推論，應明確標示。
- 若來源彼此矛盾，不可自行抹平，應保留衝突並標示差異。
- 若來源不足以支持明確判斷，應寫成待確認，不可硬下結論。

## Ingest 流程

當 `raw/` 新增來源時，Claude Code 應：

1. 確認來源位於 `D:\Karpathy\raw`。
2. 讀取來源並確認是否可正常解析。
3. 抽取關鍵概念、實體、主張、證據、衝突與待解問題。
4. 優先更新既有頁面。
5. 只有在沒有合適頁面時才建立新頁。
6. 必要時更新 `wiki/index.md`。
7. 在 `wiki/log.md` 追加紀錄。
8. 若與既有內容衝突，明確標示衝突，不直接覆蓋舊結論。
9. 以增量整合為原則，不做孤立摘要後就結束。

## Query 流程

當使用者提出問題時，Claude Code 應：

1. 先閱讀 `wiki/index.md`。
2. 再閱讀最相關的 wiki 頁面。
3. 若 wiki 不足，再回到 `raw/` 補讀原始來源。
4. 區分來源已有支持的內容、Claude Code 的整理推論、以及尚不確定部分。
5. 根據可支持內容回答。
6. 若問答中產生具有長期價值的新整理、新比較或新關聯，應回寫 wiki。
7. 將重要更新記錄到 `wiki/log.md`。

**核心原則：有價值的回答應回寫 wiki。**

## Lint 流程

Claude Code 應定期檢查 wiki 健康狀況，包括：

- 頁面矛盾
- 舊主張未更新
- 孤立頁面
- 重複頁面
- 缺少交叉連結
- 缺少來源清單
- metadata 不一致
- 命名不一致
- 可補強的重要概念缺頁

若發現問題，優先順序為：

1. 標示
2. 修補
3. 補連結
4. 補來源
5. 補索引

除非明確要求，否則不要在 lint 時任意大改整體結構。

## Log 規則

`wiki/log.md` 必須採 append-only 方式追加，不回頭覆寫。

建議格式：

```md id="dugqt6"
## [YYYY-MM-DD] ingest | 來源名稱
## [YYYY-MM-DD] query | 問題主題
## [YYYY-MM-DD] lint | 檢查摘要
## [YYYY-MM-DD] blocked | 失敗或阻塞原因

## 回報方式

對人的回報優先說清楚：

1. 檢查了什麼
2. 改了什麼
3. 還有什麼不確定

## 失敗處理

若遇到錯誤、資料不足、讀取失敗、來源損壞、路徑不明、範圍不明或衝突無法判定：

1. 先停下來
2. 明確指出問題
3. 說明已知影響
4. 提出最小下一步檢查動作
5. 不可假裝任務已成功
6. 不可用推測補齊來源空缺

若失敗具有診斷價值，應追加記錄到 wiki/log.md。

## 人與 Claude Code 的分工

### 人的工作

- 選擇與收集來源
- 決定研究方向與重點
- 提出問題
- 審閱 wiki 品質
- 判斷何時需要人工裁決

### Claude Code 的工作

- 讀取來源
- 摘要與抽取
- 編譯頁面
- 維護索引
- 維護交叉連結
- 發現衝突
- 標示不確定性
- 將高價值問答沉澱回 wiki
- 持續整理與修補知識結構